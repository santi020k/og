{
  "name": "santi020k-og-workspace",
  "private": true,
  "description": "Renderer-agnostic Open Graph image generation with Sharp and Satori adapters.",
  "license": "MIT",
  "scripts": {
    "build": "turbo run build",
    "clean": "turbo run clean && rm -rf .turbo",
    "deploy:web": "pnpm run build && wrangler pages deploy apps/web/dist --project-name=santi020k-og --branch=main",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "lint:spell": "cspell \"**\" --gitignore --cache --show-suggestions --show-context",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "validate": "pnpm run lint:spell && turbo run validate"
  },
  "devDependencies": {
    "cspell": "^10.0.1",
    "turbo": "^2.10.11",
    "wrangler": "^4.125.0"
  },
  "engines": {
    "node": ">=22.18.0"
  },
  "packageManager": "pnpm@10.32.1"
}
